export async function initThreeVeil(){const canvas=document.getElementById('three-veil');if(!canvas||matchMedia('(prefers-reduced-motion: reduce)').matches)return;try{const THREE=await import('https://cdn.jsdelivr.net/npm/three@0.164.1/build/three.module.js');const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true,powerPreference:'high-performance'}),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(45,1,.1,100);camera.position.z=8;const group=new THREE.Group();scene.add(group);const geometry=new THREE.TorusKnotGeometry(1.38,.008,220,16,2,5),material=new THREE.MeshBasicMaterial({color:0x7fe8ff,transparent:true,opacity:.38});for(let index=0;index<5;index++){const mesh=new THREE.Mesh(geometry,material.clone());mesh.scale.setScalar(1+index*.58);mesh.rotation.set(index*.45,index*.18,index*.28);mesh.material.opacity=.2-index*.024;group.add(mesh)}const resize=()=>{const width=innerWidth,height=innerHeight;renderer.setSize(width,height,false);renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));camera.aspect=width/height;camera.updateProjectionMatrix();group.position.set(width>980?2.8:.6,-.35,0)};addEventListener('resize',resize,{passive:true});resize();let frame=0;const tick=()=>{frame+=.006;group.rotation.x=Math.sin(frame)*.14;group.rotation.y+=.0028;group.rotation.z=Math.cos(frame*.7)*.08;renderer.render(scene,camera);requestAnimationFrame(tick)};tick()}catch(error){document.documentElement.dataset.threeFallback='true'}}
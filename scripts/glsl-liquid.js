export function initLiquidShader(){const canvas=document.getElementById('glsl-refraction');if(!canvas||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const gl=canvas.getContext('webgl',{alpha:true,antialias:false,preserveDrawingBuffer:false});if(!gl)return;const vertex=`attribute vec2 p;void main(){gl_Position=vec4(p,0.,1.);}`;const fragment=`precision mediump float;uniform vec2 r;uniform vec2 m;uniform float t;float n(vec2 p){return sin(p.x)*sin(p.y);}void main(){vec2 uv=gl_FragCoord.xy/r;vec2 c=uv-.5;float d=length(c);float wave=sin((c.x+c.y+t*.18)*18.)*.012+sin((c.x-c.y-t*.13)*26.)*.006;float cursor=1.-smoothstep(.0,.62,length(uv-m));vec3 col=vec3(.38,.72,1.)*(wave+.05)+vec3(.78,.95,1.)*pow(max(0.,1.-d),5.)*.16+vec3(.45,.28,1.)*cursor*.075;float a=smoothstep(.78,.08,d)*.22+cursor*.07;gl_FragColor=vec4(col,a);}`;const shader=(type,src)=>{const s=gl.createShader(type);gl.shaderSource(s,src);gl.compileShader(s);return s};const program=gl.createProgram();gl.attachShader(program,shader(gl.VERTEX_SHADER,vertex));gl.attachShader(program,shader(gl.FRAGMENT_SHADER,fragment));gl.linkProgram(program);gl.useProgram(program);const buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);const loc=gl.getAttribLocation(program,'p');gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,2,gl.FLOAT,false,0,0);const res=gl.getUniformLocation(program,'r'),time=gl.getUniformLocation(program,'t'),mouse=gl.getUniformLocation(program,'m');let mx=.5,my=.5;addEventListener('pointermove',e=>{mx=e.clientX/innerWidth;my=1-e.clientY/innerHeight},{passive:true});const resize=()=>{const d=Math.min(devicePixelRatio,1.5);canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width='100%';canvas.style.height='100%';gl.viewport(0,0,canvas.width,canvas.height)};addEventListener('resize',resize,{passive:true});resize();const tick=now=>{gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);gl.uniform2f(res,canvas.width,canvas.height);gl.uniform1f(time,now*.001);gl.uniform2f(mouse,mx,my);gl.drawArrays(gl.TRIANGLES,0,3);requestAnimationFrame(tick)};requestAnimationFrame(tick)}